

local function HoldIngredient()
    --Request server to make player hold ingredient visually
end

local function PlaceIngredient()
    --Request server to place ingredient from player's hand to counter/tray
end

local function UpdatePreview()
    --Track player´s cursor to move preview of ingredient being placed 
end
