local ReplicatedStorage = game:GetService("ReplicatedStorage")
local CreateDictRegistry = require(ReplicatedStorage.Util.Registry.CreateDictRegistry)

-- Create the registry for Data Template
return CreateDictRegistry()
